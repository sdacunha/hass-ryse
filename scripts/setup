#!/usr/bin/env bash
# Install Home Assistant + this integration's runtime deps into the active
# Python environment. Works inside a venv on your host machine; also runs
# automatically as the devcontainer's postCreateCommand if you ever bring
# one back.
set -euo pipefail

cd "$(dirname "$0")/.."

python3 -m pip install --requirement requirements.txt
